"""
因子服务器路由模块

本模块提供了因子服务器的所有 API 路由，包括：
- 因子管理（创建、查询、更新、删除）
- 因子分析（运行分析、查询结果）
- 图表数据（各种分析图表的数据接口）

核心概念
--------

- **路由**：FastAPI 路由，定义了 API 的端点和处理函数
- **因子管理**：用户因子的 CRUD 操作
- **因子分析**：运行因子分析任务并查询结果
- **图表数据**：为前端图表提供数据接口

为什么需要这个模块？
-------------------

在 Web 应用中，需要提供 API 接口供前端调用：
- 前端需要创建、查询、更新、删除因子
- 前端需要运行因子分析并查看结果
- 前端需要获取各种图表数据

这个模块统一管理所有 API 路由，提供了清晰的接口定义。

工作原理（简单理解）
------------------

就像餐厅的点餐系统：

1. **接收订单**：接收前端发来的请求（就像接收点餐）
2. **处理订单**：调用相应的服务函数处理请求（就像准备菜品）
3. **返回结果**：将处理结果返回给前端（就像上菜）

注意事项
--------

- 所有路由都是异步函数，使用 async/await
- 路由参数使用 FastAPI 的 Query 和 Body 进行验证
- 错误处理由服务层统一处理
""" 