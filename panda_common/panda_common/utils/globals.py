"""
全局变量模块

本模块定义了项目中的全局变量，就像一个"全局配置中心"，
它存储了一些在整个项目中共享的变量，方便统一管理和使用。

核心概念
--------

- **start_date**：全局开始日期，用于某些默认的时间范围设置
- **end_date**：全局结束日期，用于某些默认的时间范围设置

使用方式
--------

直接导入并使用：

```python
from panda_common.utils.globals import start_date, end_date
```

注意事项
--------

- 这些变量是模块级变量，修改会影响整个项目
- 建议只在需要全局默认值时使用
- 具体功能应该使用函数参数，而不是依赖全局变量
"""

# 全局开始日期，格式：YYYYMMDD
# 用于某些功能的默认时间范围设置
start_date = "20240505"

# 全局结束日期，格式：YYYYMMDD
# 用于某些功能的默认时间范围设置
end_date = "20240606"
